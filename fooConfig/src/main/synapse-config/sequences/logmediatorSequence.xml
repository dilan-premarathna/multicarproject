<?xml version="1.0" encoding="UTF-8"?>
<sequence name="logmediatorSequence" trace="disable" xmlns="http://ws.apache.org/ns/synapse">
    <log level="custom">
        <property name="*********" value="invoked sequence"/>
    </log>
    <payloadFactory media-type="json">
        <format>{"newValue" : "$1"}</format>
        <args>
            <arg literal="true" value="invoked sequence *********"/>
        </args>
    </payloadFactory>
    <respond/>
</sequence>
